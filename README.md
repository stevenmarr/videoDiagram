# Video Flow Line Diagram Editor

A Flask web app for creating video flow diagrams using nodes.

## Structure
- MVC-based: Models (data), Views (templates), Controllers (routes).
- Run: `python app.py`
- Future: Add DB in models.py for saving diagrams.